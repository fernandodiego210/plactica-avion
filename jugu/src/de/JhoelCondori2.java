package de;

public class JhoelCondori2 {
	if (izquierda && naveX > 15) naveX -= 3;   // Izquierda
	if (derecha && naveX < 385) naveX += 3;    // Derecha  
	if (arriba && naveY > 15) naveY -= 3;      // Arriba
	if (abajo && naveY < 485) naveY += 3;      // Abajo
	}

}
